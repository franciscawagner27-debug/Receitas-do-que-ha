import React from "react";

export default function Header() {
  return (
    <header className="bg-olive/10 border-b border-olive/30 py-4 mb-10 shadow-soft">
      <div className="max-w-4xl mx-auto px-6 flex items-center justify-between">
        {/* Espaço reservado para navegação futura (se quiseres adicionar links ou menu) */}
      </div>
    </header>
  );
}
